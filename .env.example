# Flask Secret Key for session management
FLASK_SECRET_KEY=change-this-to-a-secure-random-secret-key

# Optional: Google Gemini API Key for role-adapted live AI narrative generation
# (If not provided, the application uses structured deterministic narrative templates)
GOOGLE_API_KEY=

# Flask Environment (development / production)
FLASK_ENV=development
FLASK_DEBUG=1
